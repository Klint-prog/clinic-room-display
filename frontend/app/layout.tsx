import './globals.css'
export const metadata = { title: 'Clinic Room Display', description: 'Room display for clinics' }
export default function RootLayout({ children }: { children: React.ReactNode }) { return <html lang="pt-BR"><body>{children}</body></html> }
