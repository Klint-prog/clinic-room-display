'use client'
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { API } from '../../lib/api';
export default function Login(){ const [email,setEmail]=useState('admin@clinic.local'); const [password,setPassword]=useState('admin123456'); const [err,setErr]=useState(''); const router=useRouter();
 async function submit(e:any){e.preventDefault();setErr(''); const r=await fetch(`${API}/api/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})}); const data=await r.json(); if(!r.ok){setErr(data.error||'Erro');return} localStorage.setItem('token',data.token); router.push('/admin/dashboard')}
 return <div className="min-h-screen grid place-items-center bg-slate-950"><form onSubmit={submit} className="w-full max-w-md bg-white p-8 rounded-3xl shadow"><h1 className="text-3xl font-bold mb-2">Entrar</h1><p className="text-gray-500 mb-6">Painel administrativo da clínica</p>{err&&<div className="bg-red-50 text-red-700 p-3 rounded-xl mb-4">{err}</div>}<label className="label">E-mail</label><input className="input mb-4" value={email} onChange={e=>setEmail(e.target.value)}/><label className="label">Senha</label><input className="input mb-6" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn w-full">Acessar</button></form></div> }
